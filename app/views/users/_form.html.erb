<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, t("errors.error")) %> <%= t("errors.prohibited") %>:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label(:nick, t("activerecord.attributes.user.nick")) %>
    <%= f.text_field(:nick) %>

    <%= f.label(:pass, t("activerecord.attributes.user.pass")) %>
    <%= f.password_field(:pass) %>

    <%= f.label(:email, t("activerecord.attributes.user.email")) %>
    <%= f.text_field(:email) %>

    <%= f.label(:birth, t("activerecord.attributes.user.birth")) %>
    <%= f.date_select(:birth, start_year: 1900) %>

    <%= f.label(:lang, t("activerecord.attributes.user.lang")) %>
    <%= lang_tag(:lang) %>

    <%= f.submit %>
  </div>
<% end %>
